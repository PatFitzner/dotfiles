# Files listed here are NOT deployed. Use to exclude host-specific files.
# This is the per-host (template) ignore file; .chezmoiignore (static) exists
# alongside it for unconditional entries (docs, README, .git).

{{- if ne .chezmoi.hostname "pat-ws" }}
# pat-ws-only files (excluded on every other host):
.config/autostart/org.keepassxc.KeePassXC.desktop
{{- end }}

{{- if ne .chezmoi.hostname "omarchy" }}
# omarchy-only files (excluded on every other host):
.config/autostart/remmina-applet.desktop
{{- end }}
